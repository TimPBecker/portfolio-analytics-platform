# Database Password Secret Template
# Copy this file to .env and set your database password:
# cp .env.example .env
# Non-secret parameters (host, port, user, database) are configured in config.yaml

DB_PASSWORD=
TELEGRAM_BOT_TOKEN=
